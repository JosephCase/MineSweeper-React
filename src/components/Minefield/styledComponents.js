import styled from 'styled-components';
import Floater from '../StyledCompontents/Floater';

export const StyledMineField = styled(Floater)`
    cursor: pointer;
    flex-grow: 1;
    align-items: center;
`;